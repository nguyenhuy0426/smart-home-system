/*
 * Responsibility: declares RTSP stream lifecycle surfaces for the ESP32-CAM
 * without implementing camera or network streaming behavior.
 */
#ifndef CAMERA_STREAM_PIPELINE_H
#define CAMERA_STREAM_PIPELINE_H

const char *camera_stream_pipeline_stub(void);

#endif
