.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Commit the ESP-IDF component resolution for reproducible camera builds even
# though the parent monorepo ignores dependency lockfiles globally.
!dependencies.lock
