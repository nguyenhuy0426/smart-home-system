<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/sh_bg"
    android:clipToPadding="false"
    android:paddingBottom="18dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingTop="22dp"
        android:paddingEnd="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="Phòng"
            android:textColor="@color/sh_text_primary"
            android:textSize="26sp" />

        <TextView
            android:id="@+id/tv_rooms_home_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/sh_text_secondary"
            android:textSize="13sp"
            tools:text="Nhà của Huy" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_room_overview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:clipToPadding="false"
            tools:itemCount="4"
            tools:listitem="@layout/item_room_overview_card" />

        <TextView
            android:id="@+id/tv_rooms_empty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:background="@drawable/bg_card_rounded"
            android:gravity="center"
            android:padding="22dp"
            android:text="Chưa có phòng nào trong nhà đang chọn"
            android:textColor="@color/sh_text_secondary"
            android:textSize="13sp"
            android:visibility="gone"
            tools:visibility="visible" />
    </LinearLayout>
</androidx.core.widget.NestedScrollView>
